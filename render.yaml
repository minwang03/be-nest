services:
  - type: web
    name: be-nest
    env: node
    region: singapore
    plan: free

    buildCommand: |
      npm install
      npm run build

    startCommand: npm run start:prod

    envVars:
      - key: NODE_VERSION
        value: 20.11.1
      - key: NODE_ENV
        value: production
